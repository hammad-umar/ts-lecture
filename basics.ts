// For Writing TypeScript code two extensions are used ".ts" and ".tsx"
// ".tsx" is used when we are writing "React.js" code.
// ".tsx" = JSX + TypeScript

// We can complile the TypeScript Code into JavaScript using this command
// tsc <filename>.ts

let pi: number = 3.1416
console.log(pi)
console.log(Math.round(3.14161718))
